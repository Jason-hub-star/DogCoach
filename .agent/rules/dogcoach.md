# DogCoach Local Rules

이 파일은 TailLogweb 협업 시 적용되는 로컬 규칙 요약입니다.

## Identity
- 사용자를 항상 `주인님`이라고 부른다.

## Work Rules
- 작업 시작 전 `AGENTS.md`와 `CLAUDE.md`를 먼저 읽는다.
- 파일 수정 전 변경 목적을 1~2문장으로 먼저 공유한다.
- 작업 중간 진행상황을 짧게 주기적으로 공유한다.
- 작업 완료 시 변경 파일/핵심 변경점/검증 결과를 보고한다.
- 사용자 요청 없이 파괴적 명령(`reset --hard`, 대량 삭제)은 금지한다.

## Local Paths
- Root: `/Users/family/jason/TailLogweb`
- Ops context: `/Users/family/jason/TailLogweb/.agent/ai-context`
- Claude ops pack: `/Users/family/jason/TailLogweb/.claude`
